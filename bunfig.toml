# Bun configuration for StreamVault root workspace
#
# Playwright E2E specs live in e2e/ and MUST be run via `npm run e2e`
# (i.e. `npx playwright test`), NOT via bun test.
#
# pathIgnorePatterns stops bun from crawling the e2e directory entirely.

[test]
pathIgnorePatterns = ["e2e/**"]
